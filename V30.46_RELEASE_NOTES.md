# Bright Mind Tutor V30.46

## Focus
Production environment integration readiness without embedding or exposing credentials.

## Changes
- Fixed `/readyz` payment configuration logic so an empty webhook-secret set is not incorrectly reported as configured.
- `/readyz` now reports separate boolean states for Telebirr and CBE webhook configuration.
- Added remote deployment smoke support through `BMT_BASE_URL`.
- Provider smoke test now checks Gemini, Telebirr server configuration, and the deployed `/readyz` endpoint.
- No secret values are printed by the smoke test.
- Existing payment HMAC, amount, currency, idempotency, Firebase auth, admin claims, rate limiting, CORS, and security headers are preserved.

## Environment
Use the existing Render/server environment variables. Do not put secrets in the ZIP.

Required for Telebirr production verification:
- `TELEBIRR_API_KEY`
- `TELEBIRR_API_SECRET`
- `TELEBIRR_WEBHOOK_SECRET`
- `PAYMENT_CURRENCY`

Required for Gemini:
- `GEMINI_API_KEY`

Optional for remote smoke test:
- `BMT_BASE_URL`

## Validation
- Python compilation: PASS
- Release secret scan: PASS
- ZIP integrity: PASS
- Live provider calls: only run when credentials exist in the execution environment.
