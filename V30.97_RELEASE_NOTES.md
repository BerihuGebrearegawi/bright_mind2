# Bright Mind Tutor V30.97

## Careful 10X Quality Fixes
- Fixed a JavaScript Temporal Dead Zone bug in `student.js` where `BMT_PRICES` was read before initialization.
- Made teacher approval atomic with a Firestore batch so `teacherRequests` and `teachers/{uid}` cannot diverge on partial writes.
- Made targeted announcements compatible with legacy/current student profile fields (`grade`, `className`, `class`) with UID de-duplication.
- Synchronized runtime health/readiness version and regression-test expectations to V30.97.
- Re-ran Python compilation and JavaScript syntax checks.
