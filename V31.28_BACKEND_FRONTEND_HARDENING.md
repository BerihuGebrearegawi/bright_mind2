# BMT V31.28 — Backend + Frontend Hardening

- Backend health/readiness version aligned to V31.28.
- Student JS cache-buster aligned to V31.28.
- Service-worker shell cache bumped to V31.28 to prevent stale UI.
- PWA manifest icon paths corrected to `/static/...`, eliminating root-level 404s for Android icons.
- Render manifest explicitly declares `FIREBASE_SERVICE_ACCOUNT_JSON` as a secret environment variable. The value is not included in source.
- Existing authentication, role authorization, Firebase/Firestore contracts, payment routes, and dashboard APIs are preserved.

## Required deployment secret
Set the existing Firebase service-account JSON in Render as `FIREBASE_SERVICE_ACCOUNT_JSON`. Never commit the JSON file or its private key.
