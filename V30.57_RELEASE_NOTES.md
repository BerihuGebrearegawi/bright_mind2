# Bright Mind Tutor V30.57

## Production-safety hardening
- Readiness now reports `payment_currency_configured` and `production_safety` without exposing secret values.
- In production mode (`FLASK_ENV=production` or `BMT_ENV=production`), readiness is blocked if `ALLOW_WEBHOOK_WITHOUT_AMOUNT` is enabled.
- In production mode, `PAYMENT_CURRENCY` must be configured.
- Existing Firebase, Gemini, Telebirr/CBE webhook, authentication, payment amount, HMAC, idempotency, rate-limit, CORS, and security-header controls are preserved.
- Release artifact excludes `.env`, `.pyc`, `__pycache__`, backup files, and logs.

## Validation
- Python compilation: PASS
- Release consistency: PASS
- Public smoke-test compilation: PASS
- Secret/artifact scan: PASS
