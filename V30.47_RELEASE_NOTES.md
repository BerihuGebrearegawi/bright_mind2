# Bright Mind Tutor V30.47

## Changes
- Tightened `/readyz`: production readiness now requires Firebase, Gemini configuration, and a configured payment webhook secret.
- Updated `/healthz` and `/readyz` version to 30.47.
- Updated remote provider smoke test to enforce the same readiness contract.
- Preserved existing authentication, admin custom claims, payment HMAC/amount/currency/idempotency, rate limiting, CORS, security headers, and AI protections.

## Provider integration
- Gemini smoke test uses the Interactions API endpoint and reads `GEMINI_API_KEY` only from the process environment. Google documents the Interactions API and REST authentication via `x-goog-api-key`.
- Telebirr credentials remain environment-only. No secret values are stored in the release.

## Validation
- Python compilation: PASS
- JavaScript checks: PASS
- Secret scan: PASS
- ZIP integrity: PASS
- Live provider calls: not executed in this build environment because its environment does not contain the user's production secrets.
