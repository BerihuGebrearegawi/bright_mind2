# BMT V31.12 — Dashboard UX and Admin Preview Fixes

- Fixed landing-page logo so the image is percentage-sized inside the animated circular unit and scales with the pulse.
- Added missing teacher exam analytics containers referenced by `teacher.js`.
- Improved teacher dashboard navigation with a dedicated Exams anchor.
- Strengthened Student/Teacher admin-preview rendering with read-only demo state, clearer banners, and representative dashboard content.
- Preview mode never performs Firebase writes or destructive actions.

Validation: Python/JS syntax and duplicate-ID checks should be rerun before deployment.
