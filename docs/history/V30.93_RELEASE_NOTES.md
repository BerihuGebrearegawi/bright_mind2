# Bright Mind Tutor V30.93

## Focus
FCM diagnostics and National/Regional exam archive URL validation.

## Changes
- FCM send endpoint now returns safe actionable error categories and per-error-code counts.
- Explicitly invalid/unregistered FCM tokens are removed from `fcmTokens` automatically.
- FCM credentials, permission, and server errors are logged with request IDs without exposing secrets.
- Exam archive `previewUrl` and `downloadUrl` now require HTTPS.
- Google Drive, Cloudinary, and MEGA archive URLs are restricted to their expected hosts.
- Existing admin authorization and premium entitlement logic are preserved.

## Validation
- Python compilation: PASS
- JavaScript syntax checks: PASS
- No Firebase secrets added to client code.
