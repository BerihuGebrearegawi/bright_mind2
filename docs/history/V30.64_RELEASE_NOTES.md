# Bright Mind Tutor V30.64

## Fixes
- Fixed a critical Student Portal runtime regression: Firebase Messaging functions were used without importing `getMessaging`, `getToken`, and `onMessage`.
- Added a safe runtime guard so unsupported Firebase Messaging environments do not crash the Student Portal module.
- Preserved V30.63 password visibility UX: passwords are hidden by default and can be toggled with the visibility control.

## Validation
- Python compilation
- JavaScript static checks
- Firebase Messaging import/reference consistency
- Password input coverage
- Security/artifact scan
- ZIP integrity

Live Firebase/Gemini/Telebirr tests remain deployment-owner managed.
