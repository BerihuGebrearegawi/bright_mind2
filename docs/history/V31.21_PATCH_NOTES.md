# BMT V31.21 Patch 1

## Verified fixes
- Updated active backend health/version responses from 31.19 to 31.21.
- Updated Student and Teacher script cache-busting from `?v=31.19` to `?v=31.21`.
- Updated service-worker cache name from `bmt-shell-v31-19` to `bmt-shell-v31-21` so old dashboard assets are invalidated.
- Preserved secure Admin Preview token flow for Student, Teacher and Parent dashboards.
- Preserved existing Firebase, authentication, exams, AI and payment logic.

## Verification
- Python `compileall`: PASS.
- Node JavaScript syntax checks for Student, Teacher and Admin scripts: PASS.
- Legacy active `31.19` references in app/templates/static/service-worker: none found.

## Note
Local API endpoints that require Firebase Admin credentials can still return HTTP 503 when the local Termux environment does not contain the required Firebase service-account configuration. This is an environment configuration issue, not a Python syntax failure.
