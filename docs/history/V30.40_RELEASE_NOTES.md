# Bright Mind Tutor V30.40 Release Notes

## Baseline
V30.39

## Focus
Correctness, runtime reliability, and release validation.

## Changes
- Fixed a duplicated `try` statement in the AI Tutor route that caused `app.py` to fail Python compilation.
- Updated `/healthz` and `/readyz` reported version to `30.40`.
- Preserved V30.39 authentication, admin custom-claims authorization, rate limiting, CORS, security headers, payment controls, and AI gateway protections.
- Revalidated JavaScript syntax across the release.
- Revalidated Python syntax.
- Added V30.40 release manifest and notes.

## Validation
- Python compilation: PASS
- JavaScript syntax checks: PASS
- Release archive integrity: PASS
- Production Firebase/Telebirr/Gemini live tests: NOT AVAILABLE in this environment

## Security note
Firebase Web SDK `apiKey` values are client configuration, not Firebase service-account private keys. Server-side credentials remain environment-variable based and are not included in the release.

## Status
READY WITH CONDITIONS
