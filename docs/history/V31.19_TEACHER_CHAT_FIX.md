# BMT V31.19 — Teacher Community Chat Fix

## Fixed
- Teacher community chat no longer depends on the browser's Firestore listener for the shared community room.
- Added authenticated Flask GET endpoint: `/api/chat/community`.
- Teacher chat now loads through the server and refreshes every 5 seconds.
- Send button now uses an explicit JavaScript event listener instead of inline module handlers.
- Added Enter-to-send (Shift+Enter keeps a new line).
- Added sending-state protection to prevent duplicate sends.
- Teacher replies can now include media in the same reply.
- Added cache-busting from teacher.js v31.17 to v31.19.

## Verification
- Python compileall: PASS
- JavaScript syntax check: PASS
- ZIP integrity: PASS

## Live requirement
Render + real Firebase/Cloudinary credentials still need to be tested live.
