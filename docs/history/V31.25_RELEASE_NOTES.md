# BMT V31.25 — Secure Admin Footer Entry

## Change
- Added a small shield (🛡️) Admin entry beside the existing footer contact information in `templates/index.html`.
- The icon navigates to the existing `/admin` route.
- Kept Student, Teacher and Parent dashboard links unchanged.
- No backend, Firebase, authentication, database, payment or API logic was changed.
- Admin authorization remains enforced by the existing admin authentication/API layer. The footer icon is only a navigation entry point, not an authorization mechanism.
- Added keyboard focus styling and compact responsive presentation.
