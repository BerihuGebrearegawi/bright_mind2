# BMT V31.05 — Registration + Dashboard UX hardening

## Changes
- Student registration now has visible status feedback, disabled/loading submit state, and success/error status text.
- Teacher dashboard gets a quick-navigation bar for core areas.
- Parent dashboard gets summary cards for child count, average assessment, and attendance.
- Existing authentication, Firestore rules, parent authorization, and teacher authorization are preserved.

## Verification
- `node --check static/student.js` PASS
- `node --check static/teacher.js` PASS
- `python -m py_compile app.py teacher_routes.py parent_routes.py admin_control_routes.py` PASS

## Important
This does not claim that Firebase/Render live registration is fixed until it is tested against the deployed environment. The new UI makes the exact failure visible instead of silently appearing frozen.
