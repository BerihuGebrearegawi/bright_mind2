# Bright Mind Tutor V30.78 Release Notes

## Notification & Announcement hardening
- Fixed release regression checks to use V30.78 consistently.
- Admin in-app announcements continue to use the server-authoritative announcement endpoint.
- Optional FCM push for announcements now respects each user's `push` and `announcements` notification preferences.
- Push sending supports a server-side `target=all` mode using registered FCM devices, so the browser does not need to supply raw device tokens.
- Student notification center continues to use authenticated server APIs for list/read/read-all operations.
- Existing notification registration remains server-authoritative.

## Validation
- Python compileall: pass
- Community regression: pass
- Notification regression: pass
- Security rules regression: pass
- Static production regression: pass
- Release consistency: pass

## Production note
Authenticated Render/Firebase/FCM end-to-end delivery still requires testing in the user's live environment with real credentials.
