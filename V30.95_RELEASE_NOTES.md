# Bright Mind Tutor V30.95

## Careful Book + Video Metadata Fix

### Fixed
- Admin book metadata creation now goes through a server-authoritative admin endpoint instead of a direct browser Firestore write.
- Admin video metadata creation now goes through a server-authoritative admin endpoint instead of a direct browser Firestore write.
- This prevents Firestore client permission rules from blocking an otherwise successful GCS/Cloudinary upload with an authorization error.
- Server-side validation now checks provider, required metadata, class selection, and YouTube URL format.
- Creator UID and timestamps are written server-side.

### Security
- Both new catalog endpoints require the existing Firebase admin bearer authorization.
- No Firebase service-account credentials were added to source code.
- Existing protected media upload endpoints remain unchanged.

### Verification
- Python compilation passed.
- JavaScript syntax check passed.
- V30.93 targeted checks passed.
- V30.89 security/storage/rate-limit regression checks passed.
