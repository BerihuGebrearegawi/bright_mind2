# Bright Mind Tutor V30.53

## Focus
Release consistency and deployment QA hardening. No provider secrets are bundled.

## Changes
- Updated `/healthz` and `/readyz` version to `30.53`.
- Updated smoke-test expected version to `30.53`.
- Added `scripts/release_consistency_test.py` to detect stale release-version references.
- Preserved existing Firebase, Gemini, Telebirr/CBE, authentication, rate-limit, CORS, and security controls.

## Validation
- Python compileall: PASS
- Release consistency test: PASS
- Python smoke/provider test scripts compile: PASS
- Secret-file scan: PASS
- ZIP integrity: PASS

## Production note
Live Firebase/Gemini/Telebirr calls must be executed in the configured deployment runtime where the environment variables exist. This build does not contain credentials.
